const topics=['Algebra fluency','Equations and systems','Quadratics','Linear graphs','Measurement','Trigonometry','Probability','Formula rearranging'],diffs=['Surface','Procedural','Reasoning','Problem-solving'],modes=['No CAS','CAS optional','CAS helpful'];
const gcd=(a,b)=>b?gcd(b,a%b):a,frac=(a,b)=>{let g=gcd(Math.abs(a),Math.abs(b));return `${a/g}/${b/g}`},fmt=n=>Number.isInteger(n)?String(n):n.toFixed(1),note=m=>m==='No CAS'?'No CAS: show working clearly.':m==='CAS optional'?'CAS optional: set up the maths first, then check with CAS.':'CAS helpful: use CAS efficiently, then interpret the result.';
function Q(topic,difficulty,cas,text,solution){return{topic,difficulty,cas,title:`${topic}: ${difficulty}`,text,solution}}
const makers={
'Algebra fluency':(d,m,v)=>{let a=2+v,b=5+v,c=3+v;if(d==='Surface')return Q('Algebra fluency',d,m,`Simplify <strong>${a}x + ${b}x - ${c}x</strong>.`,[`Collect coefficients: ${a}+${b}-${c}=${a+b-c}.`,`Answer: <strong>${a+b-c}x</strong>.`,note(m)]);if(d==='Procedural')return Q('Algebra fluency',d,m,`Expand and simplify <strong>(${a}x + ${b})(x - ${c})</strong>.`,[`Expand: ${a}x^2 - ${a*c}x + ${b}x - ${b*c}.`,`Collect like terms.`,`Answer: <strong>${a}x^2 ${b-a*c>=0?'+':'-'} ${Math.abs(b-a*c)}x - ${b*c}</strong>.`,note(m)]);let p=v+2,r=v+5;if(d==='Reasoning')return Q('Algebra fluency',d,m,`Factorise <strong>x^2 + ${p+r}x + ${p*r}</strong>, then state the zero values.`,[`Numbers multiplying to ${p*r} and adding to ${p+r} are ${p} and ${r}.`,`Factorised form: <strong>(x + ${p})(x + ${r})</strong>.`,`Zeros: <strong>x = -${p}</strong> or <strong>x = -${r}</strong>.`]);let n=v+2;return Q('Algebra fluency',d,m,`Show that ${n} times the next integer, plus ${n+1}, is a square number.`,[`Next integer is ${n+1}.`,`${n}(${n+1})+${n+1}=(${n+1})(${n}+1).`,`Answer: <strong>${(n+1)**2}</strong>.`])},
'Equations and systems':(d,m,v)=>{let x=v+3,a=v+2,b=v+5,r=a*x-b;if(d==='Surface')return Q('Equations and systems',d,m,`Solve <strong>${a}x - ${b} = ${r}</strong>.`,[`Add ${b}: ${a}x=${r+b}.`,`Divide by ${a}.`,`Answer: <strong>x = ${x}</strong>.`,note(m)]);if(d==='Procedural'){let y=v+4;return Q('Equations and systems',d,m,`Solve <strong>x + y = ${x+y}</strong> and <strong>2x - y = ${2*x-y}</strong>.`,[`Add equations: 3x=${3*x}.`,`x=${x}.`,`Substitute to get y=${y}.`,`Answer: <strong>(${x}, ${y})</strong>.`])}if(d==='Reasoning')return Q('Equations and systems',d,m,`A rectangle has width x and length ${a}x + ${b}. Its perimeter is ${2*(x+a*x+b)} cm. Find x.`,[`2(x+${a}x+${b})=${2*(x+a*x+b)}.`,`${a+1}x+${b}=${x+a*x+b}.`,`Answer: <strong>x = ${x}</strong>.`]);return Q('Equations and systems',d,m,`Two numbers sum to ${2*x+3}. The larger is 3 more than the smaller. Find them.`,[`Let smaller be n, larger n+3.`,`2n+3=${2*x+3}.`,`n=${x}.`,`Numbers: <strong>${x}</strong> and <strong>${x+3}</strong>.`])},
'Quadratics':(d,m,v)=>{let r1=v+1,r2=v+4;if(d==='Surface')return Q('Quadratics',d,m,`Factorise <strong>x^2 - ${r1+r2}x + ${r1*r2}</strong>.`,[`The numbers are -${r1} and -${r2}.`,`Answer: <strong>(x - ${r1})(x - ${r2})</strong>.`,note(m)]);if(d==='Procedural'){let h=v+2,k=v+3;return Q('Quadratics',d,m,`State the turning point of <strong>y = (x - ${h})^2 + ${k}</strong>.`,[`In y=(x-h)^2+k the turning point is (h,k).`,`Answer: <strong>(${h}, ${k})</strong>.`])}if(d==='Reasoning')return Q('Quadratics',d,m,`Solve <strong>x^2 - ${r1+r2}x + ${r1*r2} = 0</strong> and explain the graph meaning.`,[`Factorise: (x-${r1})(x-${r2})=0.`,`Solutions: x=${r1} or x=${r2}.`,`These are the x-intercepts.`,note(m)]);let h=v+2,k=v+9;return Q('Quadratics',d,m,`A model is <strong>h(t)=-(t-${h})^2+${k}</strong>. Find the maximum height and when it occurs.`,[`The parabola opens downward.`,`Turning point is (${h},${k}).`,`Maximum: <strong>${k}</strong> at <strong>t=${h}</strong>.`])},
'Linear graphs':(d,m,v)=>{let x1=v,y1=2*v+1,x2=v+3,y2=2*(v+3)+1;if(d==='Surface')return Q('Linear graphs',d,m,`Find the gradient between <strong>(${x1}, ${y1})</strong> and <strong>(${x2}, ${y2})</strong>.`,[`Gradient = (${y2}-${y1})/(${x2}-${x1}).`,`Answer: <strong>${(y2-y1)/(x2-x1)}</strong>.`]);if(d==='Procedural')return Q('Linear graphs',d,m,`Find the y-intercept of <strong>y = ${v+2}x + ${v+5}</strong>.`,[`The y-intercept is c in y=mx+c.`,`Answer: <strong>(0, ${v+5})</strong>.`]);if(d==='Reasoning'){let s=v+2;return Q('Linear graphs',d,m,`Find the intersection of <strong>y = x + ${v}</strong> and <strong>y = ${2*s+v} - x</strong>.`,[`Set equal: x+${v}=${2*s+v}-x.`,`2x=${2*s}, so x=${s}.`,`y=${s+v}.`,`Intersection: <strong>(${s}, ${s+v})</strong>.`])}let c=y1-(v+1)*x1;return Q('Linear graphs',d,m,`A line passes through <strong>(${x1}, ${y1})</strong> with gradient <strong>${v+1}</strong>. Find y=mx+c.`,[`Use y=${v+1}x+c.`,`Substitute point: ${y1}=${v+1}(${x1})+c.`,`c=${c}.`,`Answer: <strong>y=${v+1}x ${c>=0?'+':'-'} ${Math.abs(c)}</strong>.`])},
'Measurement':(d,m,v)=>{let l=v+6,w=v+3,h=v+2;if(d==='Surface')return Q('Measurement',d,m,`Find the area of a triangle with base <strong>${l}</strong> cm and height <strong>${w}</strong> cm.`,[`Area=1/2 x base x height.`,`Answer: <strong>${fmt(.5*l*w)} cm^2</strong>.`]);if(d==='Procedural')return Q('Measurement',d,m,`Find the volume of a rectangular prism <strong>${l}</strong> cm by <strong>${w}</strong> cm by <strong>${h}</strong> cm.`,[`Volume=${l} x ${w} x ${h}.`,`Answer: <strong>${l*w*h} cm^3</strong>.`,note(m)]);if(d==='Reasoning')return Q('Measurement',d,m,`Convert <strong>${l*w*h*1000} cm^3</strong> to litres.`,[`1000 cm^3 = 1 L.`,`Answer: <strong>${l*w*h} L</strong>.`]);let r=v+4,ang=30+10*v;return Q('Measurement',d,m,`Find the area of a sector with radius <strong>${r}</strong> cm and angle <strong>${ang} degrees</strong>.`,[`Area=angle/360 x pi x r^2.`,`Area=${ang}/360 x pi x ${r}^2.`,`Answer: <strong>${fmt(ang/360*Math.PI*r*r)} cm^2</strong>.`,note(m)])},
'Trigonometry':(d,m,v)=>{let hyp=v+10,ang=25+5*v;if(d==='Surface')return Q('Trigonometry',d,m,`Which ratio uses opposite and hypotenuse?`,[`SOH CAH TOA gives sine = opposite/hypotenuse.`,`Answer: <strong>sine</strong>.`]);if(d==='Procedural')return Q('Trigonometry',d,m,`A right triangle has hypotenuse <strong>${hyp}</strong> cm and angle <strong>${ang} degrees</strong>. Find the opposite side.`,[`Use sine.`,`opposite=${hyp} x sin(${ang}).`,`Answer: <strong>${fmt(hyp*Math.sin(ang*Math.PI/180))} cm</strong>.`]);if(d==='Reasoning'){let o=v+4,a=v+9;return Q('Trigonometry',d,m,`A ramp rises <strong>${o}</strong> m over a horizontal distance of <strong>${a}</strong> m. Find the angle.`,[`tan(theta)=${o}/${a}.`,`theta=tan^-1(${o}/${a}).`,`Answer: <strong>${fmt(Math.atan(o/a)*180/Math.PI)} degrees</strong>.`,note(m)])}let base=fmt(hyp*Math.sin(ang*Math.PI/180));return Q('Trigonometry',d,m,`A ${hyp} m cable makes an angle of ${ang} degrees with the ground and starts ${v} m above ground. Find the total height reached.`,[`Cable height=${hyp} x sin(${ang})=${base}.`,`Add ${v}.`,`Answer: <strong>${fmt(Number(base)+v)} m</strong>.`])},
'Probability':(d,m,v)=>{let r=v+2,b=v+3,g=v+5,t=r+b+g;if(d==='Surface')return Q('Probability',d,m,`A bag has <strong>${r}</strong> red, <strong>${b}</strong> blue, and <strong>${g}</strong> green counters. Find P(blue).`,[`Total=${t}.`,`Answer: <strong>${frac(b,t)}</strong>.`]);if(d==='Procedural')return Q('Probability',d,m,`Using the same bag, find P(not green).`,[`Not green = red or blue = ${r+b}.`,`Answer: <strong>${frac(r+b,t)}</strong>.`]);if(d==='Reasoning'){let p=.1*(v+2),n=100+25*v;return Q('Probability',d,m,`A spinner has P(yellow)=<strong>${p.toFixed(1)}</strong>. It is spun <strong>${n}</strong> times. Estimate yellow results.`,[`Expected frequency=probability x trials.`,`Answer: <strong>${p*n}</strong>.`])}let both=v+2,a=v+8,bb=v+7,N=v+24,at=a+bb-both;return Q('Probability',d,m,`In a class of <strong>${N}</strong>, <strong>${a}</strong> play basketball, <strong>${bb}</strong> play tennis, and <strong>${both}</strong> play both. Find P(neither).`,[`At least one=${a}+${bb}-${both}=${at}.`,`Neither=${N}-${at}=${N-at}.`,`Answer: <strong>${frac(N-at,N)}</strong>.`,note(m)])},
'Formula rearranging':(d,m,v)=>{let b=v+4,h=v+3,A=b*h/2;if(d==='Surface')return Q('Formula rearranging',d,m,`Substitute <strong>b=${b}</strong> and <strong>h=${h}</strong> into A=bh/2.`,[`A=${b} x ${h}/2.`,`Answer: <strong>${fmt(A)}</strong>.`]);if(d==='Procedural')return Q('Formula rearranging',d,m,`Make <strong>h</strong> the subject of <strong>A=bh/2</strong>.`,[`Multiply by 2: 2A=bh.`,`Divide by b.`,`Answer: <strong>h=2A/b</strong>.`,note(m)]);if(d==='Reasoning')return Q('Formula rearranging',d,m,`A triangle has area <strong>${A}</strong> cm^2 and base <strong>${b}</strong> cm. Use A=bh/2 to find h.`,[`h=2A/b.`,`h=2 x ${A}/${b}.`,`Answer: <strong>${h} cm</strong>.`]);let s=v+8,t=v+2;return Q('Formula rearranging',d,m,`Using d=st, distance is <strong>${s*t}</strong> km and time is <strong>${t}</strong> h. Make s the subject and find speed.`,[`s=d/t.`,`s=${s*t}/${t}.`,`Answer: <strong>${s} km/h</strong>.`])}
};
const bank=[];topics.forEach(t=>diffs.forEach(d=>modes.forEach(m=>{for(let v=1;v<=3;v++)bank.push(makers[t](d,m,v))})));let filtered=[...bank],idx=0,seconds=0,timer=null;const $=s=>document.querySelector(s),topicFilter=$('#topicFilter'),difficultyFilter=$('#difficultyFilter'),casFilter=$('#casFilter'),timerSelect=$('#timerSelect'),timerDisplay=$('#timerDisplay'),solutionPanel=$('#solutionPanel');
function fill(sel,vals){sel.innerHTML='<option value="All">All</option>'+vals.map(v=>`<option value="${v}">${v}</option>`).join('')}function filters(){filtered=bank.filter(q=>(topicFilter.value==='All'||q.topic===topicFilter.value)&&(difficultyFilter.value==='All'||q.difficulty===difficultyFilter.value)&&(casFilter.value==='All'||q.cas===casFilter.value));idx=0;render()}function render(){let q=filtered[idx]||bank[0];$('#bankCount').textContent=bank.length;$('#questionMeta').innerHTML=`<span class="pill">${q.topic}</span><span class="pill ${q.difficulty==='Problem-solving'?'hard':''}">${q.difficulty}</span><span class="pill cas">${q.cas}</span> Question ${idx+1} of ${filtered.length}`;$('#questionTitle').textContent=q.title;$('#questionText').innerHTML=q.text;$('#solutionText').innerHTML='<ol>'+q.solution.map(s=>`<li>${s}</li>`).join('')+'</ol>';solutionPanel.hidden=true;resetTimer()}function resetTimer(){clearInterval(timer);seconds=Number(timerSelect.value);tick();if(seconds>0)timer=setInterval(()=>{seconds--;tick();if(seconds<=0)clearInterval(timer)},1000)}function tick(){if(seconds<=0&&Number(timerSelect.value)===0){timerDisplay.textContent='--:--';return}let m=Math.floor(Math.max(0,seconds)/60),s=Math.max(0,seconds)%60;timerDisplay.textContent=`${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`}fill(topicFilter,topics);fill(difficultyFilter,diffs);fill(casFilter,modes);[topicFilter,difficultyFilter,casFilter].forEach(s=>s.addEventListener('change',filters));timerSelect.addEventListener('change',resetTimer);$('#shuffleBtn').addEventListener('click',()=>{filtered.sort(()=>Math.random()-.5);idx=0;render()});$('#prevQuestion').addEventListener('click',()=>{idx=(idx-1+filtered.length)%filtered.length;render()});$('#nextQuestion').addEventListener('click',()=>{idx=(idx+1)%filtered.length;render()});$('#revealSolution').addEventListener('click',()=>{solutionPanel.hidden=!solutionPanel.hidden});render();
